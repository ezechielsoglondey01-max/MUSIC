{
  "name": "music-browser",
  "version": "1.0.0",
  "description": "Express app that fetches music from a public API, stores it in MongoDB, and exposes a web UI to browse it.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "mongoose": "^8.19.1"
  }
}

